{
  "name": "dogecoin-price-prediction-mern",
  "version": "1.1.0",
  "private": true,
  "description": "MERN Dogecoin prediction dashboard powered by CoinGecko market data.",
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -k -n API,WEB -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "start:api": "npm --prefix backend start",
    "preview": "npm --prefix frontend run preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "git": "^0.1.5"
  }
}
